import 'package:flutter/material.dart';

class WeatherHomeAppBar extends StatelessWidget with PreferredSizeWidget {
  const WeatherHomeAppBar({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }

  @override
  // TODO: implement preferredSize
  Size get preferredSize =>  const Size(0.0, 56.0);
}
